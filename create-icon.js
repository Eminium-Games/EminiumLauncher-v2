# Create a simple 256x256 PNG icon for Linux
# This is a workaround since we can't convert the ICO file directly

# We'll create a simple placeholder icon that electron-builder can use
# The user can replace this with a proper icon later
